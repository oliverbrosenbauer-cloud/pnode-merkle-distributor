node_modules/
.keys/
state.json
web/mdk.bundle.js
web/claim-demo.html
# docs/index.html is committed on purpose — it is the published demo page.
# It embeds five devnet test keys (worthless play money) so the demo works
# without setup. The project wallet in .keys/ is NOT in here.
